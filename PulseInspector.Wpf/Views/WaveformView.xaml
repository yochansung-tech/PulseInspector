<UserControl x:Class="PulseInspector.Wpf.Views.WaveformView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="Transparent" Padding="8">
        <Grid>
            <Canvas x:Name="PlotCanvas" SizeChanged="PlotCanvas_SizeChanged" Background="Transparent"/>
            <TextBlock x:Name="EmptyText" Text="No waveform selected" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.55"/>
        </Grid>
    </Border>
</UserControl>
