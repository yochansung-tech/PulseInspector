<Window x:Class="PulseInspector.Wpf.Views.TrainingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Training" Width="720" Height="520" MinWidth="600" MinHeight="420"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Training Model" FontSize="24" FontWeight="SemiBold"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,18,0,12">
            <TextBlock Text="Normal groups:" VerticalAlignment="Center"/>
            <TextBlock x:Name="CountText" Margin="8,0,24,0" FontWeight="SemiBold" VerticalAlignment="Center"/>
            <TextBlock Text="Required:" VerticalAlignment="Center"/>
            <TextBlock x:Name="RequiredText" Margin="8,0,24,0" FontWeight="SemiBold" VerticalAlignment="Center"/>
            <TextBlock Text="Confidence:" VerticalAlignment="Center"/>
            <TextBlock x:Name="ConfidenceText" Margin="8,0,0,0" FontWeight="SemiBold" VerticalAlignment="Center"/>
        </StackPanel>
        <DataGrid Grid.Row="2" x:Name="IssuesGrid" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Severity" Binding="{Binding Severity}" Width="90"/>
                <DataGridTextColumn Header="Feature" Binding="{Binding FeatureName}" Width="120"/>
                <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <DockPanel Grid.Row="3" Margin="0,14,0,0">
            <TextBlock x:Name="StatusText" VerticalAlignment="Center" Foreground="{DynamicResource MutedTextBrush}"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Validate" Click="Validate_Click"/>
                <Button Content="Train" Click="Train_Click" IsDefault="True"/>
                <Button Content="Close" Click="Close_Click" IsCancel="True"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
